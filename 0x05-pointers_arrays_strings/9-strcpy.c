#include "main.h"
#include <string.h>
/**
 * _strcpy -  copies the string pointed to by src
 * @dest: strign1
 * @src: string2
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
